# name: Multi Environment Deployment Pipeline

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:

#   build:

#     runs-on: ubuntu-latest

#     outputs:
#       build_version: ${{ steps.version.outputs.version }}

#     steps:

#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Generate Build Version
#         id: version
#         run: |
#           VERSION="v${GITHUB_RUN_NUMBER}"
#           echo "version=$VERSION" >> $GITHUB_OUTPUT
#           echo "Generated version: $VERSION"

#       - name: Build Application
#         run: |
#           echo "Building application version ${{ steps.version.outputs.version }}"


#   deploy-dev:

#     runs-on: ubuntu-latest
#     needs: build

#     environment: dev

#     steps:

#       - name: Deploy to Development
#         run: |
#           echo "Deploying ${{ needs.build.outputs.build_version }} to DEV environment"
#           sleep 5


#   deploy-staging:

#     runs-on: ubuntu-latest
#     needs: deploy-dev

#     environment: staging

#     steps:

#       - name: Deploy to Staging
#         run: |
#           echo "Deploying ${{ needs.build.outputs.build_version }} to STAGING environment"
#           sleep 5


#   deploy-production:

#     runs-on: ubuntu-latest
#     needs: deploy-staging

#     environment: production

#     steps:

#       - name: Deploy to Production
#         run: |
#           echo "Deploying ${{ needs.build.outputs.build_version }} to PRODUCTION environment"
#           sleep 5